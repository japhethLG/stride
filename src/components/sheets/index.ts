/**
 * Sheets barrel (CP4). The mobile bottom-sheet shell (CLAUDE.md §9). Concrete
 * sheets (CP5) wrap `BaseSheet` and register with the sheet host.
 */
export { BaseSheet, type BaseSheetProps } from "./BaseSheet";
